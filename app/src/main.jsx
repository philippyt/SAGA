import React from "react";
import ReactDOM from "react-dom/client";
import MainFront from "./main_front";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MainFront />
  </React.StrictMode>
);
